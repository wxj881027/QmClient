# 赞助头衔预览文字容器清理

## 问题与范围

2026-09-19 的断言日志显示 `text container was not empty`，残留文本包含 `[赞助头衔]`。源码确认成品预览和风格列表将文字容器保存在静态缓存中，却没有接入菜单的窗口重建清理流程。日志不能确定触发重建的具体用户操作。

用户已确认：补齐两类缓存的清理与重建，保留头衔外观和设置；补回归测试代码，只运行 quick 源码门禁，不编译、不执行测试。

## 实现与验收

- 菜单窗口重建、退出时统一删除成品预览和全部风格条目的文字容器，包括已折叠或滚出屏幕的条目。
- 成品预览使用可被清理的共享句柄；删除会重置句柄，下次绘制走原有创建分支。
- 风格列表继续使用原有逐项重建路径。
- 手工验收场景：打开赞助头衔设置并展开风格列表，再折叠或离开页面；调整窗口或切换语言后不触发断言，返回页面仍可正常显示预览。
- 新增两项源码回归测试，检查清理生命周期接线与成品预览重建分支。未编译、未运行测试，也未做实机验收。
- 按当前工作树版本递增补丁号：3.9.5 → 3.9.6。

## 审查与验证

- Findings：只读审查未发现本次补丁的阻断问题。删除接口同时释放缓冲并重置句柄，重复清理空句柄安全；窗口重建在引擎断言检查前调用菜单清理。
- `git diff --check`：通过。
- `python qmclient_scripts/gate/check_gate.py --mode quick --report-json-path tmp/title_preview_cleanup_gate.json`：PASS，11 项通过，0 警告，0 失败。
