# 远程区间第二阶段吸收 · 台账（**活文档**）

**区间口径（用户 2026-09-23 重申）**：只以远程仓库的 `2535814bd7e8916bcc78c609ee24461f7e73e51a..153bd251f8f8cd7b9f2060dc1551e4ef24db9e05` 为提交来源，`git log --reverse` 实查 42 个提交，按第一父净改动及最终存活意图逐项判断。`3c4782e50` 是倒数第二个合并节点，不是实际终点。此范围是工作审计基线；当前 `origin/dyl_dev` 与 `upstream/master` 的 Git merge-base 已是后来的 `8f2f4b188`，不能把工作审计区间当成 PR 的 compare 范围。

**作业位置**：隔离 worktree `E:\Coding\DDNet\QmClient-local-first`，现有分支 `merge/dyl-dev-remote-functional-2026-09-20`。此前按原禁令手工编辑；2026-09-23 用户解除自动合并禁令并授权提交当前成果以便后续合并。仍不推送、不建 PR。

---

> **本文件是活文档**：只保留指令依据、当前状态、剩余工作与操作纪律。
> **历史明细已归档**至 `2026-09-22-远程吸收历史明细（归档）.md`（表 1–表 13、10.1–10.195），需追溯时再查。
> **当前更新见本文件第一节及归档 10.263**。旧轮次的「待核」和文件/行差统计是当时的调查线索，不作为当前代码剩余工作量。

## 〇、原始指令（用户原文，**一切判定的依据**）

> 本节置顶的目的：**防止目标漂移**。此前多次出现的偏差（把已定事项写成"待裁决"、凭 diff 片段反推结论、把文案与引用它的代码分批做）都是因为没有回到这些原文。

| # | 指令原文 | 含义与落地 |
| --- | --- | --- |
| 1 | "**注意，避免 git 的自动合并**" | 原始限制，2026-09-23 用户已明确解除自动合并禁令并允许提交中间检查点；保留本行原话作历史依据。仍不得回退其他成果、推送或建 PR。 |
| 2 | "远程的工作按照代码的意图去理解，没有意外都得吸收进来。即便是代码结构冲突，也得要手工改为更加合理的结构，以此作为吸收。**如果本地没有对应渲染路径，是当新功能吸收**。" | **吸收是默认动作**；结构冲突 → 手工改成更合理的结构（不是拒绝吸收的理由）；本地无渲染路径 → 当新功能吸收。 |
| 3 | "文档相关的，大部分是文档的新增（spec 吧）然后删除了的，可以不合并。（注意意思是，**仅**文档里面的；如果一个提交里面有文档和别的工作，那么，**别的工作也不能落下来**）" | 纯文档提交可跳过；**混合提交里的非文档工作必须吸收**。 |
| 4 | "每一个 R 或者 N，不是硬性的需要跑 gate 和测试和构建的，比如文档的没动运行时代码的不用跑。如果动了的话，那也得等到**一整个 R 或者 N 的工作完成后再跑**" | 按**整项 R/N** 为单位验证，不在半途反复跑；纯文档不跑。 |
| 5 | "后续不要问我和阻塞了……你先出一个完整的调研的方案，再来按照这个进行，**后续不再提问和中断**" | **不得再抛裁决项、不得阻塞**；先调研出方案再执行。 |
| 6 | "一轮能不能快一点，而且一轮做的事情能不能多一点啊" | 每轮尽量多做、做完整。 |
| 7 | 已裁决的吸收集合 | **吸收**：N2 非运行时部分、N3、N5、N8、N9、N11、N14、N16、N17；**保留本地**：N1、N4、N6、N7、N12、N13、N15、N18、N19。 |
| 8 | 用户后续明确（本文件曾误记为"待裁决"） | **N13 是本地工作 → 保留**；**`perf/frame` 攒批是远程工作 → 吸收远程版替换本地**；**运行时验证在所有合并完成后统一做，非阻塞**。 |
| 9 | "全部完成后更新报告台账表 8 状态并交用户 review" | 收口后才做表 8 最终更新（表 8 属历史明细，见归档文件）。 |

**区间与作业位置**：见文首「区间口径」。**判定顺序**：系统/开发者指令 > 用户当前指令 > 仓库规则；本表 1–9 条为用户对本任务的原始与后续指令，**优先于任何我事后写在台账里的推断**。

---

## 一、当前现场（2026-09-24 收口快照）

**当前快照（2026-09-24 收口，实测）**：`HEAD` 仍为 `94c2a55ed821172c8e9758063c2bf0ca1b2f0571`，远程审计范围 `2535814bd..153bd251f` 实测 42 个提交。本轮按用户裁决修正两处：**Zen/焦点模式保留**（本地源码仍在用 `qm_focus_mode`，4 个文件）、**速通计时器删除**（远程 #29 `402a4b89e` 已删，终点 `153bd251f` 无 `qm_speedrun_timer`）。未提交改动不视为已提交或已验证，也不回退、不纳入本任务提交。

**本轮新增证据**：

| 项 | 证据 | 结论 |
| --- | --- | --- |
| 图标 MTSDF 脚本迁移核对 | 新 `qm_icon_msdf_official.py`(139 行) / `qm_build_icon_msdf_official.py`(48 行) 与删除的旧文件 `diff` 仅差第 2 行 docstring 改名说明；入口引用已切换；CMake 只剩 `qm-icon-msdf-atlas`；`data/qmclient/nameplate_msdf/` 与 `nameplate_msdf/` 组件目录均不存在 | 逐字节迁移成立 |
| 逐文件闭合矩阵 | `tmp/closure_matrix.tsv`：42 提交全部第一父差异的非文档路径，逐路径对照终点 blob 与当前工作树哈希。终结点存在、本地缺失的 **81 项已全部归因**（见下）；终结点已删、本地保留的 **12 项**全部是图标 morph 族（远程 #4/#5 增、#29 删，本地按裁决保留） | 无遗留的「文件级漏搬」 |
| 速通计时器删除 | 14 个源文件 + 门禁脚本 + i18n：`config_variables_qmclient.h`(7 配置)、`hud.{cpp,h}`、`menus.h`、`menus_qmclient.cpp`(6 处)、`QmCardCatalog{.cpp,.h}`、`QmCardCatalogHud.cpp`(3 处)、`QmCardRenderBridge.cpp`、`QmCardRegistry.cpp`、`QmModuleLayoutAdapter.cpp`、`QmModuleTypes.h`、`qm_card_registry_test.cpp`、`qm_module_layout_adapter_test.cpp`、`check_settings_ui_migration.py` | 代码/测试/门禁/文案同步删除 |
| i18n 条目清理 | 删除 31 条已无源码引用的运行时条目：qmclient.toml 22、tclient.toml 7、misc.toml 2（`TIME EXPIRED!`、`Speedrun timer`）。`_migrations/` 历史迁移记录与 `migrate_chinese_keys_to_english.py` 按规则**保留未动** | 运行时孤儿清理 |
| 静态自检 | 已删符号全仓零残留；14 个改动文件花括号平衡；`menus_qmclient.cpp` 8 个 `switch(Id)` 全有 `default`；**发现并修正硬编码 `QmModuleCount` 42→41**（删除枚举值后必须同步，否则默认表与测试不变量不一致） | 编译不可用时的替代自检 |

**本轮验证矩阵**：

| 项 | 本轮证据 | 结论 |
| --- | --- | --- |
| i18n `extract_strings.py` | 退出码 0；5742 个 source key（较清理前 5757 少 15） | 通过 |
| i18n `generate_all.py` | 退出码 0；12 个 `data/languages/*.txt` 重生成，5742 key | 通过 |
| i18n `validate.py` | 退出码 0；12 份产物覆盖校验通过，`blocking violations: 0`；保留 4 条长度警告、335 项人工审阅积压 | 通过 |
| 门禁 default（`--run-qm-smoke`） | 第二次完整运行 **15 通过 / 0 警告 / 0 失败 / 0 跳过 / PASS**；包含 4 个进程 smoke、9 个 E2E 场景、C++ 与 Rust 全量 | 通过 |
| Python 测试（三个根） | `qmclient_scripts/tests` 89 项（跳过 9）OK；`qmclient_scripts/gate/tests` 47 项 OK；`qmclient_scripts/integration` 15 项 OK | 通过 |
| `game-client` / `run_cxx_tests` / `qm-music-helper` / Rust 全量 | Windows 封装脚本均退出码 0；C++ **3254/3254（490 suites）**，Rust 全量通过；`qm-music-helper` 无新增工作且目标成功 | 通过 |

**81 项「终结点有、本地无」逐文件归因**（无一遗留为真代码缺口）：

| 类 | 数量 | 归因 |
| --- | --- | --- |
| 子模块 gitlink | 5 | `ddnet-libs`（本地保留含 WavPack 的 gitlink，10.199）、`vendor/minhook`、`vendor/msdfgen` —— 本地均存在，脚本因无法 hash 目录而误报 |
| 本地按 AGENTS.md 拆分的测试 | 68 | 远程单体 `qmclient_monitoring_test.cpp` / `qm_hud_notifications_test.cpp` / `QmIconAtlasTest.cpp` / `render_target_test.cpp` → 本地拆为 `qmclient_monitoring_*_test.cpp`、`qm_hud_notifications_*_test.cpp`、`qm_icon_*_contract_test.cpp`、`render_target_contract_test.cpp` |
| 远程位图图标资产 | 4 | `qm_icons_*_{1x,2x,4x}.{json,png}`、`qm_icons_thin_msdf.*`：本地按裁决只用 MTSDF，权重集合 `regular/bold/fill/light/duotone` 与 assets、CMake、`qm_icon_manager.cpp`（Thin→light 映射）、契约测试四处自洽 |
| 脚本链差异 | 4 | `qm_build_icon_msdf.py`（SVG 链）/ `qm_build_icon_atlas.py` 及其测试：本地用字体链 `qm_build_icon_msdf_font.py` + 官方 `msdf-atlas-gen` |
| 已内联的代码 | 4 | `glyph_atlas.h` → `text.cpp` 内 `CAtlas`(108 行) 与扩容只清新区域(466/468)；`glyph_atlas_image.h` 同处；`sprite_image.h` → `graphics_threaded.cpp` 的 `GetSpriteImageRect`；`QmMapUploadSearch.h` → `qm_map_upload.h` 同名结构 |
| 明确不吸收 | 2 | `src/engine/shared/qm_removed_config.h` 只清洗 `qm_focus_mode_*`，而 Zen 保留，清洗会破坏保留功能；`test_removed_qmlive_and_superpowers.py` 要求删 `.agents`，与本仓 skill 入口冲突 |

**42 行提交表当前结论**：已具备提交范围、第一父口径、终点存活意图，以及上面按路径的**逐文件**闭合证据（81 项 LOCAL_GONE 全部归因、12 项 LOCAL_KEPT 结论明确）。当前代码、构建、全量测试、门禁和 i18n 均已收口；剩余仅为真实服务、真实桌面/设备和跨平台人工验证，不能由自动测试替代。

### 历史检查点（不作为当前结果）

以下内容保留用于追溯：`acf31219d` 的 `game-client`、3242/3242 和 quick gate 11/0/0，以及第 178/179 轮的 `OpenEditor()` 构建失败、3180/3180、quick gate 10/0/1 和覆盖层 5/6。它们均早于当前代码与当前未提交改动，不能覆盖本节的收口矩阵。

## 一、旧现场与历史定位（2026-09-23）

**最近已验证的代码检查点**：`acf31219d` 的 `game-client` 构建退出码 0、C++ **3242/3242（488 suite）**、quick gate **11/0/0**。其后已提交的补丁及当前设置卡改动尚未统一构建/测试，不能将旧绿结果称为当前工作树通过。`qm-music-helper`、Rust、i18n 及覆盖层/性能脚本的旧结果只作历史证据，代码收口后按第七节统一刷新。下表为第 178 轮旧故障，**只作历史定位**。

**2026-09-23 收口增量**：#24/#27 的终点动效与状态栏缓存已经按 10.259 补齐；#29 设置卡动画改用连续绘制几何（10.260）、在线分布断线保留快照已接代码和行为测试（10.261）。#40 定向审计报告的进程高优先级属于已确认的本地删除，Saiko+ 属用户要求删除的额外快速输入模式，均不作为漏吸收；分类清单门禁回归已补，卡片译文差异留最后 i18n。新增代码尚未重验。

最新扫尾：#27 另补移动方块单遍绘制与强制性能阶段日志绕过明细限流；#40 的媒体封面移交和浏览器人数状态行为测试已登记。#1 余下指定路径未见新的确定漏项。大型提交仍有未逐分支核对的路径；代码收口后统一验证，真实视觉/服务留人工。

| 目标 | 结果 |
| --- | --- |
| `qm-music-helper` | 第 132 轮退出码 0；**当前代码状态未重验** |
| `game-client` | **失败**：`menus_qmclient.cpp:3627` 调用 `CInputOverlay::OpenEditor()`，远程替换后的类无此成员；组合构建退出码 1 |
| `run_cxx_tests` | 单独运行退出码 0，**3180 / 3180（470 suite）**；不等于 `game-client` 构建通过。测试数量较第 176 轮少 6 项，源于删除旧覆盖层测试并加入远程 5 项，不能把旧数字沿用 |
| `quick gate` | **10 通过 / 0 警告 / 1 失败 / FAIL**：`config_variables_qmclient.h` 缺 EOF 换行 |
| i18n 链 | 第 177 轮 extract/generate 退出码 0；本次 `validate.py` 全量扫描通过（12 个语言文件，blocking 0；4 条长度风险警告、396 条人工审阅积压），不代表后续新增文案已覆盖 |
| 覆盖层 Python 测试 | `python -m unittest qmclient_scripts.tests.test_input_overlay_layout`：**5/6**；`test_runtime_restores_percentage_position_controls` 失败，菜单未消费 `QmInputOverlayPosX/PosY` |

| 项 | 说明 |
| --- | --- |
| 第 179 轮静态修正（未验证） | 覆盖层菜单移除已失效编辑器按钮、增加 X/Y 位置滑块；高度函数和断言改为六行，配置头补 EOF 换行。聊天日志后台队列及单测已接入；`CMakeLists.txt` 已登记新测试 |
| 下一步 | 浏览器极窄窗口地图文本宽度和重叠拖拽把手已补静态防护；#24/#27 的导航与语义动效参数、状态栏本地时间测量缓存已补终点差异，待统一构建与目视；并行复核 #1/#29 与 #40 的剩余精确路径。逐提交判定后统一运行构建、C++/脚本测试、quick gate、i18n 链，最后做人工场景验证 |


## 二、已完成（第二阶段吸收）

| 族 | 内容 | 条目 |
| --- | --- | --- |
| **配置与门控** | 表 10 权威清单（配置变量集合比对）中的缺口逐项落地：控制台日志分类过滤、钩子倒计时+echo 合并窗口、跳跃提示默认文案中文化+迁移标记、回放显示选项 `QmDemo*` 5 项、名牌昵称显示范围、他人超大/发射表情开关、称号系统绘制层与配置 `QmTitle*` 12/14 项等 | 10.7–10.19 |
| **卡片目录重构（B1）** | Phase A 11 文件落地 + `EQmModuleId` 与注册表补齐；Phase B 三页（VisualDeck / HudDeck / FunctionDeck）全部切换；5 项测试失败修复；i18n 缺口闭合；**B1 结项**（四项全绿） | 10.28–10.49 |
| **B2 缓存/预计算族** | `qm_outline_neighbors.h`、`route_visited.h`+`route_start_index.h`（含**拒绝一处附带语义变化**）、`nameplate_text_cache.h`（真实意图是**修缺陷**）、`snapshot_entities.h`、皮肤预备区（双路径上传+去重+缺陷防护，**保住本地空白素材回退**）、广播 markdown 磁盘缓存、`spectator_tele_search.h` 整功能、`rounded_rect_directions.h` | 10.51–10.69 |
| **结构冲突手工合并** | `emoticon_commands.h`（**保住本地 `super_emote`**）、钩子提示线裁剪（保留本地更精确的方向性裁剪） | 10.72、10.74 |
| **N8（B3）** | `CColorVertex` 类型 + `SetColorVertex` 接口，**9/9 调用点逐槽核对转换**（`graph.cpp`、`moving_tiles.cpp`、`render_map.cpp`、`menus_settings.cpp` 六段渐变条）；另一半 `perf_diagnostics.h` 吸收 + 配置快照写入接通 + 脱敏链 **7 个行为测试** | 10.83–10.90 |
| **好友爱心** | `friend_heart_icon.h` 吸收 + `nameplates.cpp` 改实体爱心；其余 4 处判定**本地已等价**（MSDF 图标族自带 `fill` 实心权重） | 10.92–10.95 |
| **R1（酷狗/QQ 音乐）** | **代码工作全部完成**：源与协议层 12 文件（哈希一致）+ 新建 `qm-music-helper` 目标 + 7 个协议单测；集成层全部 8 层（头文件逐字节一致 → 作业化基础设施 → `SImpl` → 生命周期 → provider 三来源化 → 4 配置项+`SyncHookConfiguration` → 前置依赖 `SelectLatestStartedLine` → 载入核心+三接口）；i18n 8 条 key × 12 语言；注册表两行；UI 接线（酷狗安装/恢复按钮+状态标签） | 10.96–10.108、10.120–10.132 |
| **核查判定为已完成（勿重复做工）** | R3 搜索条目（10.112）、R5 字形光栅化→导出链（10.115）、N2 非运行时部分（10.116）、N14（10.117）、N16 CTrails（10.78）、N17 聊天导出后台化 + N11 语音 WebSocket（10.79） | 见左 |
| **判定不需吸收** | N13（本地工作，保留）；字形图集的旧“全等价”判定已撤销，见下行 | 10.193 |
| **字形图集小项** | 远程小块 `find` 查询、扩容只清新区域、饱和描边跳出已吸收到本地内置 `text.cpp`，未构建/目视/测性能 | 10.193 |
| **构建修复** | 修掉我自己引入的 `game-client` 链接失败（登记错目标）与 guard/clang-format 冲突；7 次脚本化复原全部作废，改手工 read+edit 解决 | 10.98、10.103–10.107 |

## 三、按提交的现状表（区间 `2535814bd..153bd251f`：`3c4782e50` 的 41 个提交 + 纯删除提交 `153bd251f`）+ 待办清单

### 从头到尾的提交总表（`git log --reverse`，42 个；2026-09-23 校正）

本表的“待核”不是拒绝吸收：旧表把存在同名模块、测试拆分或合并节点直接当作整提交已完成，证据不足。逐提交以**对照终点仍存活的非文档意图**判定；中途新增后又撤回的工作不重复实现。合并提交按**第一父提交差异**审查，不因 `Merge` 标题跳过；下表中的文件数量只是定位线索，不是剩余工作量。

**逐文件闭合已用机器矩阵完成（2026-09-24，取代此前“代表性路径”口径）**：`tmp/closure_matrix.py` → `tmp/closure_matrix.tsv` 对 42 个提交逐个取第一父差异、筛除非文档路径，再与终点 blob 及当前工作树哈希逐一比对。终结点存在而本地缺失 81 项已**全部归因**（子模块 gitlink 5、本地拆分测试 68、远程位图图标资产 4、脚本链差异 4、已内联代码 4、明确不吸收 2），终结点已删而本地保留 12 项全部为图标 morph 族。明细见归档 10.265。本表各行仍是提交级意图与落地说明；`DIFF`（两侧字节不同）只表示本地已分叉，不等于漏搬。

| # | 提交 | 远程工作（摘要） | 当前现场与收口动作 |
| --- | --- | --- | --- |
| 1 | `a967e3e42` | Axiom 计分、基线清理 | 本地计分链、门禁与资源消费有静态证据；sanitizer 抑制文件及 CI 路径已补。#1 的他人 Axiom 弹窗在远程终点已撤回，勿恢复。QmLive 删除、服务端普通计分/RCON/惩罚、编辑器协作与画笔和 CI 子簇已静态核对（10.245）；本轮引擎/脚本定向复核未见确定漏项（10.249），大型基线其余精确路径仍不能称整项闭合 |
| 2 | `52100dcc6` | Merge master | 第一父 29 个非文档路径已按终点复核（归档 10.227）：CI/Android/Rust 链接、输入/滑条、背景/皮肤/Gores 生产意图当前树有实现；WebP 从现场编译改预编译库。远程 macOS 默认 Vulkan 与本地后续明确转向 Metal 不等价，保留本地平台策略并标跨平台须人工；旧源码合同撤回不冒充运行时测试 |
| 3 | `f8a227787` | UI/HUD/监控/弹道测试 | 终点存活的 Vulkan 小窗、弹道、菜单细项有本地接线；tune-zone 图标/岛摘要在终点撤回。静态初审无确定功能缺口，图形与 UI 实测留最终验证 |
| 4 | `d67a5a641` | 网易云 Hook 与交互 | 终点存活的 DLL/注入、CDP、v5 共享内存与歌词消费者、配置及构建登记均有当前链路（归档 10.238）；本地快照每三帧读取属时序差异，真实进程注入/恢复须人工 |
| 5 | `309fd789d` | 歌词 API 转 DLL 注入 | 第一父终点净项已按 DLL/bootstrap/helper、Hook 开关/超时/路径、媒体与歌词消费者复核；旧歌词 API 已由远程撤回，本地亦无；静态闭合，真实网易云进程须人工（归档 10.238） |
| 6 | `a009daced` | Merge DLL 注入改造 | 第一父非文档路径按终点复核：Hook/Bootstrap/CDP、v5 协议与 SMTC/歌词消费者、构建和测试接线均在本地；旧 `qm_lyrics/` 删除与终点一致，独立歌词卡后续又恢复，本地亦有；四个根目录 `.obj` 被远程撤回。未发现明确存活漏补（归档 10.230），真实网易云进程须人工 |
| 7 | `7ee0fee61` | 汽水/酷狗/QQ 歌词、Lyrics 模块 | 第一父存活的采集/协议/解析 32 源文件与终点 blob 一致；映射发布、超时读取、三个来源开关、HUD、注册表、helper 构建/复制/打包及设置页均有当前消费者（归档 10.237）。静态闭合；真实播放器 DLL 安装/恢复与歌词显示须人工 |
| 8 | `3aba2ca43` | bind HUD、HUD Pro 卡片 | 第一父 74 路径已按终点复核：v3 配置迁移、Spotify 协议/采集与歌词消费、四项 bind 状态卡、聊天弹窗/补全、字体与 CMake/测试接线在当前树均有消费者或等价实现（归档 10.225）。远程后续删自定义 bind 列表，本地在其上有独有加固，作为本地扩展保留并明确记录差异；跨进程歌词与 UI 视觉须人工 |
| 9 | `eef2d9dc5` | 重建前快照（性能/岛/翻译） | **16 条源码/测试路径已按第一父补丁复核**：主循环五段性能计时由本地演进后的泵/更新分段及快照预测阶段承接；动画中断弹簧/速度继承迁至 `QmAnimationBackend`；灵动岛两阶段弹簧与 morph、深飞 bind 同键判定、名牌缩放重建预算、Tee 眼睛单行/卡片测量、Spotify 输入及 UI 诊断降采样均有当前调用/测试。`二师兄` 中途名单在远程终点已撤回；其余 16 条是翻译来源/生成缓存、`AGENTS.md` 规则，i18n 全量验证见 10.221。仅静态/自动化验证，岛动画和名牌缩放目视须人工 |
| 10 | `0275aefe8` | 线性缩放与字体缩放 | 与 #20 合并看终点净效果：`qm_zoom_linear`、`qm_settings_font_scale`、对应相机/设置页分支及测试已在远程撤回，当前树同样不存在；不恢复中途功能。见归档 10.226 |
| 11 | `af22132ab` | 行高、副标题、中断续接修复 | 行高/副标题独立字体倍率和相机以 `m_Zoom` 起步均被 #20 撤回；终点与当前树都采用宽度驱动的卡片测量、原始副标题字号及贝塞尔续接。其他格式/测试演进见 #12/#20；真实平滑视觉须人工 |
| 12 | `27cc62c5c` | 格式、CMake 链接、审查修复 | 相机 `ZoomProgress`/`CameraSmoothingProgress` 的零时长防护与进度 clamp 被 #20 撤回，终点和当前树均无；不作为漏补。`qm-nmt-tests` plain 链接已由 #13 修正，设置页纯函数参数与各调用/测试在 #20 撤回，余下是格式化；当前 `game-client`/C++/quick gate 最近检查点通过。终点净项静态闭合（归档 10.236） |
| 13 | `36966717f` | `qm-nmt-tests` 链接签名 | **静态等价**：目标四处 `target_link_libraries` 及全局 `OUR_FLAGS_LINK` 都是 plain 签名；最终构建仍待统一验证 |
| 14 | `e1fd23eb1` | SMTC 非 Windows `g_Config` | **静态等价**：`config.h` 已在 `SYSTEM_MEDIA_CONTROLS_WINRT_ENABLED` 外引用；逐提交预览的文本冲突不代表功能缺失 |
| 15 | `fc8f15ede` | SMTC 非 Windows `IClient` | **静态等价**：`engine/client.h` 已在非 WINRT 分支可见；非 Windows 构建仍待统一验证 |
| 16 | `2f8a3733d` | 旧 Vulkan 头版本兼容 | **静态等价**：本地已有 `#ifndef VK_API_VERSION_1_4` 与两条 `VK_MAKE_*` 兼容分支，最终跨平台构建待验证 |
| 17 | `55e70cb44` | CUi 测试桩、网易云链接 | 第一父仅两路径：`qm_netease_bootstrap.cpp` 已登记在网易云 helper 与测试目标，CUi `PixelSize`/`RenderGaussianBlur` 的测试桩迁至 `ui_rect_test_helpers.cpp` 并纳入目标，旧文件只留迁移注释；静态闭合，真实进程留末尾人工 |
| 18 | `15c7a8b96` | Merge PR#182 | 第一父 20 路径与 #19 大量重复；终点存活的 Vulkan 旧头、非 WINRT 媒体头、NMT 源/链接签名和 UI 测试桩去重在本地已有。线性/独立字体缩放被 #20 撤回，无存活漏补（归档 10.230）；非 Windows 构建须人工 |
| 19 | `3eb0a04f7` | Merge origin/master | 与 #18 第一父 20 路径中 18 个补丁 ID 相同；另两条是 NMT 源登记差异及空测试桩注释，终点净项本地等价或已按 #20 撤回。无新增存活漏补（归档 10.230） |
| 20 | `5c4f10b34` | 撤回线性/字体独立缩放 | 第一父 17 路径复核：相机回到单一贝塞尔缩放，设置页内容测量只接收宽度，菜单调用与测试不再传独立字体倍率；当前树与终点的这组语义一致。本地相机另有玩家反向缩放演进，不覆盖；视觉平滑须人工。见归档 10.226 |
| 21 | `b69589d4c` | Vulkan 设备丢失与 fault 诊断 | 第一父仅 `backend_vulkan.cpp`：工作线程显存分配失败不进入帧恢复、可选 `VK_EXT_device_fault` 探测/启用/记录、旧头条件编译均有当前调用点（归档 10.231）；代码静态闭合，真实设备丢失与跨平台须人工 |
| 22 | `ef1fe786b` | 图形致命错误的诊断恢复 | 第一父六路径的后端致命错误消费、主循环在提交前记录报告并重启、资源重置接口和双模糊接口在当前树均有调用/实现；本地恢复按实际失败后端计数并选择可用候选，替代远程强制 OpenGL。静态生产链闭合，设备丢失/恢复与跨平台须人工；资源广播无触发点属双方共有风险，见 #23 |
| 23 | `f6af8eb4d` | 图形资源纪元与设备重建 | 第一父八条生产路径的句柄纪元/监听、字体纹理重传、游戏图片与图标图集重载均有当前实现和注册（归档 10.235）；终点和本地都未调用 `NotifyGraphicsResourcesReset()`，属于双方共有的运行时重建缺口，留设备丢失人工验证与后续独立修复，不计远程漏搬 |
| 24 | `8f2f4b188` | 铭牌 MSDF、灵动岛弹簧 | 最新裁决删除名牌 MTSDF，专属图集/构建/运行时/测试链已移除（未构建）；**自定义字体保留**。#24 的中心标题服务目录已在远程终点整体迁出、本地同样不含；编辑器图层边界/画笔与测试接线静态等价（归档 10.239）。岛通知/两阶段弹簧/着色器及 Axiom 计分板子簇有当前消费者（归档 10.240）；其他未审路径继续核对，视觉须人工 |
| 25 | `1edf6b732` | Markdown 广播、输入覆盖层、去 MSDF | Markdown 广播保留本地缓存与静态公告兜底；开发者草稿发布入口/HTTP 状态已补。覆盖层旧菜单断点已手工修正但未验证；名牌 MSDF 删除已按新裁决处理，控制台分类、称号色段、钩线目标缓存及其他目标项已按终点核对（归档 10.241）；静态闭合，真实广播与 UI 视觉须人工 |
| 26 | `c5b4217ee` | 称号系统、五形态拖尾 | `QmTitleAdvanced/Bloom`、空间效果绘制、设置页/预览及 profile 风格字段源码已接；名牌动态称号现调用远程存活的前缀宽度缓存。四个称号渲染/风格源文件与终点一致，聊天已有调用；五形态拖尾本地几何已演进，未发现可直接搬的缺口。**未构建/运行** |
| 27 | `3413f1376` | 自有服务 WebSocket、语音等合集 | 语音音频 WS 收发已有队列/拒发清理/包长校验；旧 `CQmRealtimeChannel` 已移除，账号及匿名专用 WS 已接。图形故障报告现先标识实际失败后端，恢复按报告后端计数/轮换、无候选停止重启及偏好快照已落代码；按钮预布局/按压动画及轮廓配置颜色预计算已补。最近检查点已构建/自动测试，真实服务未联调；账号/匿名 WS 与语音均无 UDP 后备。运行期心跳/退避只在创建时生效、协议只在首次 Connect 写入，远程终点同样如此；本地旧 URL 迁移覆盖显式默认新键是独有风险，另记待修（归档 10.242），其余净项继续核对 |
| 28 | `18758b8cd` | 匿名表情实时通道 | 独立匿名 WS 的 hello、临时身份、发送/接收、来源与玩家过滤及表情消费者已接；最近检查点已自动构建；#28 中途账号 WS 表情分支被 #34 替换，中心服目录在终点迁出；当前独立匿名 WS 及特效消费者静态闭合（10.244），真实服务须人工 |
| 29 | `402a4b89e` | 音乐/WS/聊天/好友/性能大型合集及删除 | 自动聊天日志、存档确认/双人恢复、DDRace 进度、demo 布局、好友首次静默基线、音乐 helper 拷贝/打包及 Tee 皮肤双击目标应用已接；Tee 三卡 v9 迁移及下拉弹层不透明样式已补。聊天积压淘汰/清空时同步释放导出元数据，文本性能诊断关闭时跳过高精度计时；SettingsCard 标题/副标题已接菜单文字池，Windows TXT 导出恢复平台换行（10.248），皮肤统计单遍采集、空后台表早退/计数熔断和头像卸载释放已补（10.250），开关倒计时环下限与终点一致（10.252），浏览器名称/地图列按实际剩余宽度分配且恢复比例手柄（10.254），均尚未构建/测试。Zen、速通计时器及图标 morph 属明确保护的本地卡片/扩展，名牌 MTSDF 按新裁决删除；serverbrowser/text/friends/chat/console 与名牌文字预算子簇经第一父和终点定向核对，其余存活净项继续逐文件复核，真实服务与视觉未验证 |
| 30 | `ce319ea50` | 纯文档删除 | 按用户要求不吸收纯文档历史，无非文档待办 |
| 31 | `92812f098` | VS 增量数据库删除 | 本地无该构建缓存文件，无功能待办 |
| 32 | `388f5a0b3` | 表情碰撞与浏览器列布局 | 表情碰撞、聊天 echo、浏览器布局主要链已接；聊天 emoji 缩放/基线、梦列排序接线及 15px 行高已补。`shot_emote` 单次强制发射与选择器 TAB 模式/蓄力各有实际调用，完整视觉未验证 |
| 33 | `6fdc036ab` | Linux crashdump 符号 | **静态等价，无须重复应用**：本地已有无条件定义；逐提交三方预览虽零冲突，却会另加两个条件分支定义，造成重复符号。非 Windows 构建待最后统一验证，详归档 10.183 |
| 34 | `e514d9a6f` | 表情同步切匿名 WS | 本地匿名表情改为独立常开 WS，无 UDP 后备；服务器/身份过滤已接，最近检查点已自动构建；账号入口拒收表情、匿名 hello/收发/ping/重连和服务端过滤已按终点核对（10.244）；无特效 UDP 后备，真实双端联调须人工 |
| 35 | `f6003af99` | Merge master | 27 个非文档文件已逐项静态审计；选择器蓄力、投射物窄通道保护、HUD 录制红点、浏览器单层列表四处真实缺口已落代码，尚未构建/测试/视觉验收 |
| 36 | `73a1583af` | 匿名表情跨地址同步 | 地址规范化、玩家名重映射及 `qm_websocket_log` 下的收发/失败/丢弃诊断已接匿名 WS 队列；跨地址规范化、同名玩家重映射及诊断静态闭合（10.244）；远程地址规范化行为测试已并入 `qm_realtime_test.cpp`，尚未构建/运行；跨地址玩家重映射的真实双端联调仍须人工 |
| 37 | `b407dfa9b` | 启动加载跑图进度条 | 启动页实际调用 `RenderProgressBarWithTee`，静态接线具备，运行时视觉未验证 |
| 38 | `87a5d3b1a` | 赞助页 Tee 作者展示 | 赞助卡通过 `QmSponsorAuthors::Authors()` 绘制 Tee 作者行，静态接线具备，运行时视觉未验证 |
| 39 | `f15a62135` | Merge PR#254 | **静态等价**：第一父在 `crashdump.cpp` 的补丁与 #33 完全相同，本地已有无条件单一定义；不能再次应用，非 Windows 构建留末尾 |
| 40 | `d297a1eb0` | 卡片目录、Low 帧、服务器浏览器等 | 搜索页直接编辑、图标预布局、性能 JSON 尾指针追加、Function 卡测量及浏览器行为测试已补；`QmMapUploadSearch.h` 本地异路径等价。FavoriteMaps 按实际搜索行数更新布局版本；“梦”列头已接 `SORT_QM_CLIENTS`。远程固定地图上传目标 `https://shengyan.art/api/upload` 已设为本地端点默认值，保留用户自定义覆盖；好友在线列表的 ping 更新修订号和回归测试已补。分类页与搜索页原缺目录折叠回调，现补自定义按钮、配置同步，搜索页保留 Locate 头动作（10.247）；服务器列表名称/地图分配与拖拽另有 #29 漏补（10.254、10.257）。引擎/脚本 36 路径中 33 路径意图闭合、3 翻译源文件留最终 i18n 阶段；UI 卡测量、动画、监控等分叉已有等价或保留本地差异的证据（10.253）。卡片皮肤指标头的 CMake 源清单登记已补（10.258）。旧 96 条内容分叉是定位快照，不等于未完成工作数；browser/favorites/地图搜索/性能日志、OpenGL 数组清理、UDP 读取预算、粒子批次等其他已核子簇有实现（10.233、10.243），大型提交其他路径仍须逐项复核 |
| 41 | `3c4782e50` | Merge master（原提示终点） | **静态等价**：第一父在 `crashdump.cpp` 的补丁与 #33/#39 完全相同，本地已有单一定义；无新净项，非 Windows 构建留末尾 |
| 42 | `153bd251f` | 删除 website/island-layout（对照终点） | `website/` 本地未引入；`island-layout/` 在工作区已删。该修订只删除 17 文件，当前无存活新增功能 |

**统计口径**：42 个提交，上表“待核”仍包括跨提交共用的净项，不能当作独立功能数。不能沿用旧表“29 已收口 / 3 剩余 / 9 无需操作”，也不沿用旧的 31 行计数。表 13 的按提交缺席行数及表 12-A/12-C 仅作定位，须按当前工作树重算并逐文件写出依据后才能确认每行收口。

2026-09-24 现场补记：`console.cpp` 的聊天导出元数据在积压淘汰、待处理积压淘汰及清空时同步释放；`text.cpp` 在性能诊断关闭时跳过四处高精度计时；服务器浏览器 ping 改变好友服务器延迟时推进好友列表修订号。#40 的地图上传默认目标恢复为远程 `https://shengyan.art/api/upload`，保留本地可配置端点。当前树 `game-client` 构建退出 0、C++ 全量 **3242/3242（488 suite）**、quick gate **11/0/0**。上述具体补丁属于 #29/#40 的补漏，**不能据此把大提交及 42 行整体标为逐文件闭合**；真实上传、WSS/语音、视觉与跨平台仍按末尾人工验证处理。

`ddnet-libs` 远程 gitlink `24a9a5e3` 不在此 worktree 的子模块对象库，但主工作区同名子模块保有两端对象。实际树差仅本地 `6914a425` 额外保留 21 个 `wavpack/` 文件，主工程仍使用 WavPack；依用户保留本地独有工作，维持本地 gitlink，不切换（归档 10.199）。

### 旧待办清单（只作功能索引；收口范围以正序表 + 第七节逐文件矩阵为准）
> 输入覆盖层已换成远程代码基础，菜单与布局随后手工修正但未重新构建；Zen/焦点模式和眼睛图标 morph 保留本地，铭牌 MTSDF 按最新裁决删除，自定义字体保留。下面的历史四目标绿判据统一推迟到**全部代码完成后**，不再逐项触发构建。



| # | 事项（自解释） | 是什么 / 为什么还缺 | 完成判据 | 风险 | 属哪个提交 |
| --- | --- | --- | --- | --- | --- |
| ✅1 | **计分板 HUD 布局函数化**（`score_hud_layout.h`） | 远程把计分板 HUD 的方框/名次/人物坐标抽成纯函数 `QmScoreHudLayout(...)` 供 `hud.cpp` 复用；本地仍是内联实现（`hud.cpp:1756`，`PosSize` 固定 16），远程版改为 `max(16, RankTextWidth + Split + TeeSize - ImageSize)`（布局行为改进）。耦合检查已完成，本地 `src/test/QmLayoutTest.cpp`（346 行）可承接其单测 | 新增头文件 + `hud.cpp` 改用该函数（方框/模糊底/名次/人物坐标一致）+ 远程两个测试通过 + 四目标绿 | 低 | #40、#29 |
| ✅2 | **地图投票难度缓存**（`map_vote_difficulty.h`，79 行） | 远程新增模块，**只缓存菜单展示难度**，不改投票选项与行为；本地无此文件 | 先判本地是否已有等价展示路径 → 吸收或记为组织差异 | 低 | #40、#29 |
| ✅3 | **性能报告文件日志**（`perf_file_logger.h`，98 行） | 远程 `qm_perf_debug` 诊断链的落盘部分；本地已吸收 `perf_diagnostics.h` 且快照写入已接通（10.88–10.90），但没有文件日志 | 与待办 4 同批（同属诊断链） | 低 | #29 |
| ✅4 | **`perf/frame` 逐帧日志改攒批**（`CQmPerfFrameBatch`） | 远程把逐帧阶段日志改成攒批提交以压缩体积。**这是远程工作、本地没做过 → 必须吸收替换**；此前以"会变重复埋点"暂缓的判断**作废** | 以远程版替换本地逐帧日志；`qm_perf_debug` 配置位与 `qmclient_scripts/perf/` 脚本链一致性确认；四目标绿 | 中 | #29、#40 |
| 源码已接线 5 | **称号设置页 + 空间效果消费端**（`QmTitleAdvanced`/`QmTitleBloom`） | 配置、名牌 FreeType 消费端、设置页高级控件/风格预览与服务端风格持久化已接；实时 profile 的缺席/显式空风格解析有回归测试。**未构建/运行，且整提交余项待核** | 统一构建/测试/i18n、运行时设置页/名牌视觉验证，复核提交 #26 其余文件 | 中 | #26 |
| 源码已接线 6 | **灵动岛倒计时环贴圆角矩形轮廓** | 本地已有 `RoundedRectPerimeterPoint`、`DrawOutlineRingPass/Fallback`、`m_OutlineRingThickness/Offset`、两种 SDF shader 的周长映射；旧判定“本地为 0”错误 | 逐项对照远程测试与 shader 参数语义；代码收口后统一测试，视觉须人工 | 中 | #29、#40 |
| 源码已接线 7 | **好友族分组 UI 接线** | `spectator_friend_priority.h` 的稳定分区已接入普通旁观者列表，保留本地 Rank Ghost 分支；好友分类拖放、进服宽限状态机也已接线并补独立测试，均未构建/运行 | 逐文件复核本簇其余消费者，统一构建/测试/i18n 后核对满员布局与拖放视觉 | 中 | #40 |
| ✅8 | **`prepared_media_art.h`**（WinRT 媒体素材预备） | 远程用 WinRT 准备媒体封面素材，本地无对应路径；10.81 已给出完整接线图 | 先判本地是否已有等价实现（本地媒体岛自有路径）→ 吸收或记为组织差异 | 中 | #29 |
| ✅9 | **可选清理**：R1 集成层本地死代码 | `ContainsAsciiInsensitive`/`IsSodaSourceAppId`/`ParseMediaId` 共 58 行 + 2 个未用成员，本地残留、当前不影响构建门禁 | 删除后四目标绿 | 极低 | #7 |



### 独立审计：清单之外的残留（第 135 轮机器判定）

**历史快照（第 135 轮，不是当前待办数量）**：对 2535814bd..153bd251f 的每个变更文件，用当时本地 git hash-object 与两侧 git rev-parse 比对，分三类。第 177 轮已替换覆盖层且此后有未提交成果，必须重新生成并逐文件确认，不可直接沿用下列数字或“100% 未吸收”判断。

| 类 | 含义 | 规模 | 状态 |
| --- | --- | --- | --- |
| **A** | 远程新增、本地缺失的代码文件 | **42 个**（另 31 个为图标资产 / i18n TOML / perf TS / 脚本） | 待逐文件判定 |
| **B** | 本地停在区间起点、远程改过 | **36 个代码文件**，区间改动 **499 插入 / 361 删除** | **100% 未吸收** |
| **C** | 本地已分叉且远程新增行缺席 | 以测试文件为主（远程源码合同测试，本地按 AGENTS.md 不照搬） | 待确认其背后运行时功能 |

**B 类含真实运行时功能，须先做**：monitoring/monitoring_device_perf.cpp(+248)、voice/voice_capture_pipeline.cpp(−239)、particles.cpp/h、tclient/background_particles.cpp/h(62)、UiOverlays.h(49)、SettingsCardDeckLogic.cpp(42)、QmScroll.cpp/h(32)、map_history.cpp/h(23)、UiMotion.h(24)、QmMotion.h(27)、base/perf_timer.h(12)、hud_notifications.h(13)、stutter_diagnostics.h(11)、settings_runtime_cache.cpp/h(9)、QmAnimResolve.cpp(12)、engine/friends.h、effects.h、QmTree.h、QmModuleLayoutAdapter.h、SettingsCardGeometry.h、UiContext.h。

**A 类体量大者**：QmAnimationBackend.cpp(789)+.h(243)、map_progress.h(452)、local_saves.h(417)、glyph_atlas.h(215)、local_save_display.h(171)、QmMapUploadSearch.h(113)、friend_enter_tracker.h(94)、friend_online_tracker.h(90)、sprite_image.h(89)、qm_removed_config.h(88)、friends_category_drag.h(86)、qm_sponsors.h(71)、demo_cut.h(62)、qm_chat_log_jobs.h(61)、prepared_media_art.h(52)、qm_hook_coll_visibility.h(49)、demo_ui.h(44)、glyph_outline.h(40)、qm_title_color.h(38)、glyph_atlas_image.h(23)、QmCardCatalogSkinMetrics.h(18)，另有 12 个测试文件与 5 个 perf TS。

**当时的执行顺序（已过期）**：B 类 → A 类运行时头 → C 类背后的运行时功能 → A 类测试。当时记录的已收口 B 类包括 `stutter_diagnostics.h`（10.135）、`base/perf_timer.h`、`engine/friends.h`、`effects.h/.cpp`、`tclient/map_history.h/.cpp`（10.137）。当前顺序以 7.3 为准。

**已判定为本地等价（有据，非跳过）**：远程 `engine/client.h` 的 `OnQmPerfFrame(double)` / `OnQmPerfStop(bool)` 虚函数与 `CGameClient` 覆写，本地用自有结构达成同一可观察行为：帧侧由 `gameclient.cpp:2076` 在 update 路径调用 `ProcessQmStutterFrame()`，`OnShutdown()` 与「采集被关掉」分支分别结算 stutter 窗口（`EQmStutterFlushReason::SHUTDOWN/DISABLED`）。故本项只吸收 `perf/frame` 攒批与会话生命周期，不重复接一条等价通路。

### 历史编号说明（**不再用于规划**）


`N*` / `R*` / `B*` 三类编号都是**历史遗留**，来源如下，仅用于追溯归档：

| 编号 | 出处 | 原本含义 |
| --- | --- | --- |
| **N1–N19** | 最早那份远程区间报告的**"拒绝吸收"清单**（归档表 4） | 报告初版逐项给出"拒绝理由"，后经你逐项拍板：部分推翻改为吸收、部分维持保留本地 |
| **R1–R7** | 同一报告的**"未完成缺口"清单**（归档表 5） | R1 酷狗/QQ 来源采集、R2 性能余项、R3 搜索条目、R5 聊天导出后台化、R6 真实 WSS/语音/上传端到端验证、R7 窗口顶层与键盘焦点 |
| **B0–B8** | 旧执行方案的**批次划分** | B5/B7 曾记录为代码完成；B4（语音 WS）当前仍未收口，见 7.2；这套编号只作历史索引 |

**当前规划以第三节正序提交表及 7.3 逐文件矩阵方案为准**；历史待办与旧批次不构成完整范围。

## 四、运行时验证（**非阻塞**，全部合并完成后统一做）

R1 跨进程采集、Markdown 缓存磁盘往返、旁观传送搜索、新增 i18n 文案界面显示、圆角/渐变视觉、好友爱心非默认权重、配置快照脱敏、R6/R7。**本环境无法执行的部分届时如实标注为须人工**，不提前卡住合并工作。

## 五、操作纪律（本会话付代价学到）

1. **改 `CMakeLists.txt` 后必须构建一次 `game-client`** —— 本任务统一放在全部代码收口后；`quick gate` 不构建游戏本体，`run_cxx_tests` 通过也不等于游戏可构建。
2. **禁止在同一文件上反复运行"先删后插"脚本** —— 删除步骤无从得知哪些已复原；10.104–105 的 7 次脚本尝试全部失败且两次自毁成果，最终靠"读原文 → **多行组合**锚点 → 手工 edit"解决。
3. **遇到构建/链接问题，先查本仓库同类做法** —— zlib 的解法本地早有"`find_package(ZLIB)` 后统一补 helper"的既定块，我却自己发明补法错了三轮。
4. **单行锚点常不唯一**，edit 必须用多行组合锚点。
5. **绝不在未验证的前提下声称通过**；文档类改动与运行时代码改动分别对待。
6. **diff 的方向性结论必须落回原文验证** —— `git diff <rev>:<path> -- <path>` 中 **`-` 是远程、`+` 是本地**；仅凭 hunk 片段推断两侧容易**整体反向**（10.127 把 `OnUpdate` 三处方向写反，10.129 直读文件才发现）。
7. **批量判定"某批 key/数据是否已存在"必须逐个判断**，不能用该批中的一个作哨兵代表全体（10.130 因此误判"已存在"而跳过；同类错误还有 10.112 的"只 grep 一个文件就下结论"）。
8. **i18n 改动遵循本地生成链**：`extract_strings.py` → `generate_all.py` → `validate.py`，本任务统一放在全部代码收口后；`data/languages/*.txt` 是**产物、不手改**；新文案与所属代码须对应，最终生成时补齐译文。
9. **跨文件依赖要在动手前查清**：R1 的 `OnUpdate` 依赖本地不存在的 `SelectLatestStartedLine`（10.128）与 2 参 `Start`（10.126）——**未先查依赖就照抄必然编译失败**。做法：先 `git grep` 符号在两侧的有无，再决定批次组成。
10. **台账要保持"活文档 + 归档"**：活文档只放指令依据、当前状态、剩余工作与纪律并每轮更新；轮次明细一律进归档。**3712 行的单文件曾直接导致判定漂移**（把已定事项读成待裁决），故不得再把明细堆回活文档。

## 六、交接要点（会话转接用）

**判定纪律**：区间新增行与本地已有行的对比是「行文本集合差 + 空白归一化」，本地换一种写法实现同一逻辑也会全部计为缺席，**不是工作量**。逐文件判定口径见归档 §10.176 的表 12-A / 12-C 与按提交汇总的表 13。

**已结案的决定**：

| 决定 | 内容 |
| --- | --- |
| 输入覆盖层 | **以远程设计为基础**（用户第 177 轮指令）；本地矢量布局实现与其测试已删除，测试按远程并入 |
| Zen/焦点模式 | **保留本地**，不吸收远程删除 |
| 速通计时器 | **删除**（用户 2026-09-24 明确：Zen 留下，timer 删掉）。远程 #29 已删，终点无 `qm_speedrun_timer`；本地同步删除代码、卡片、门禁登记与 18 条文案 |
| 眼睛图标 morph | **保留本地**（本地已扩展 RenderMorphFrames + CrossProgress） |
| 铭牌 MTSDF | **按最新裁决删除**本地独有的名牌 MTSDF 专属链；自定义字体、图标 MSDF 和共享制图工具保留 |
| perf/frame 攒批 | 用远程版替换本地（已吸收） |
| 图标 MTSDF 与字体 | 只删除名牌 MTSDF；保留图标 MTSDF，优先图集绘制、不可用时回退随包字体图标；保留 `qmclient/fonts/` 递归识别和自定义字体。图标烘焙范围另按实际资源契约判定 |
| 官方文档 | 保留本地 `docs/官方文档/`；`docs/官方/` 是本地重复加入，不属于本次远程 42 提交，已删除副本 |

**旧现场纪律（已被 10.181 部分取代）**：此前禁止自动合并及补丁工具。当前可用文件级自动三方合并候选辅助吸收；现有 438 条未提交状态仍不得回退或清理，逐字节搬运仍须 hash 核对。

**当前文件定位矩阵（第 179 轮初始快照，待刷新）**：`tmp/remote_absorption_inventory_2026-09-23.tsv` 覆盖对照终点的 872 个净变更路径及其第一父提交关联；本轮按当前磁盘对旧行重算，远程 blob 一致 304、远程删除 246、仍同旧快照但非远程字节 240、旧快照后变化 35、起点字节 3、本地无文件 44。后 44 包括旧路径的图标资源/脚本及本地异路径实现，不是 44 项待搬工作；重算只是定位，不改变旧 TSV 的 829 行 `UNREVIEWED` / 12 行 `PARTIAL_REVIEWED` 判定，仍不能给出精确剩余工作量。审过的依据刷新时不得丢失。

## 七、执行方案与批次状态（原《2026-09-22-远程吸收执行方案》已并入本文件并删除）

### 7.1 区间端点口径（权威）

| 项 | 内容 |
| --- | --- |
| 区间 | **`2535814bd7e8916bcc78c609ee24461f7e73e51a..153bd251f8f8cd7b9f2060dc1551e4ef24db9e05`**；逐个远程提交 42 个；`3c4782e50` 为其前一个合并节点 |
| 审计口径 | 逐文件「行文本集合差（空白归一化）」= 区间新增行 − 本地已有行；**本地换写法实现同一逻辑也会计入缺席**，只作定位用，不等于工作量（明细见归档表 12-A/12-C、表 13） |
| `website/` | 远程在区间内先加（`d297a1eb0`）后删（`153bd251f`），本地从未有 → **无需动作**（按指示：新增后删除的不合并） |
| `island-layout/` | 由**远程**提交 `8f2f4b188` 引入（已是本地 HEAD 祖先），远程于 `153bd251f` 删除 → **本地按意图删除，已完成**（工作区中 `island-layout/*` 均为 ` D` 状态，目录已不存在） |

### 7.2 批次状态（B0–B8）

| 批次 | 内容 | 状态 |
| --- | --- | --- |
| B0 | N20：删 `website/`、`island-layout/` + 口径修正 | ✅ 已完成（见 7.1） |
| B1 | N3+R3：`QmUi/cards/` 卡片目录迁移、两卡（`qm:emoticons`、`qm:map_upload`） | ✅ 已完成（见第二节） |
| B2 | R2 剩余 + 12 区性能族（`rounded_rect_directions`、`snapshot_entities`、`route_*`、`nameplate_text_cache`、`skin_prepared_*`、`score_hud_layout`、`spectator_tele_search`、`map_vote_difficulty`、`markdown_cache_writer`、`quad_rotation_cache` 等） | ✅ 已完成（见第二节） |
| B3 | N8：`SetColorVertex` 颜色提交接口替换（9/9 调用点逐槽核对）+ `perf_diagnostics` 链 | ✅ 已完成（见第二节） |
| B4 | N11：语音音频 WebSocket 迁移 + 服务端实时通道 | 语音收发静态复核已接：二进制包、8 条发送队列、拒发清理状态、包长校验；旧 UDP 后备已移除。账号/匿名专用 WS、编辑器协作及诊断有接线，其他调用链若发现具体缺失补丁继续吸收；真实服务联调留最后，不能称运行时通过。账号/匿名 WS 与语音均无 UDP 后备；旧 URL 迁移已限制为“新键为空时才迁移”，未发现覆盖显式新键的静态缺口，配置组合仍留人工核验。 |
| B5 | N2/N9 剩余（脚本、`SERVICES.md`、存活文档） | ✅ 已完成（第 26–27 轮；`tests/test_removed_qmlive_and_superpowers.py` 判定不吸收——它要求删 `.agents`，而本仓库以 `.agents/skills/` 作 skill 入口） |
| B6 | 称号设置页 + 空间效果消费端、好友分组 UI、岛轮廓核对 | 称号菜单/名牌/聊天动态效果、名牌前缀宽度缓存、旁观者/好友进服与分类拖放、赞助名单 WS/缓存/UI 已接；岛首次展开、圆环兜底及圆角矩形周长几何/双 shader 已补。代码路径静态可见，具体新增缺口再按补丁处理；均未构建/测试/目视。 |
| B7 | R1：酷狗/QQ/汽水/网易云音乐采集（`qm-music-helper`、协议层 12 文件、5 测试、`music_hooks.toml`） | ✅ 代码工作已完成（见第二节；`.qmb` 等运行时链路须人工验证） |
| B8 | R6/R7：真实 WSS/语音/地图上传端到端、窗口顶层与键盘焦点 | **须人工**：本环境无外部服务与交互桌面，只能完成可控前置 |

### 7.3 完成整个合并任务的执行方案（当前指令）

| 阶段 | 要做的事 | 收口证据 |
| --- | --- | --- |
| 1. 固定提交范围 | 固定 `2535814bd..153bd251f` 的正序 42 提交、终点存活的非文档补丁与当前工作树；合并提交按第一父差异看，侧支和后续撤回去重。旧 A/B/C、872 路径与缺席行数仅用于找补丁，不作为待办数量。 | 42 行提交表记录实际远程意图与本地处理；无需为了字节不同制造功能任务。 |
| 2. 消除已知集成断点 | 以远程覆盖层为基础手工统一菜单：移除已无实现的编辑器入口，接入水平/垂直位置控件，更新卡片高度与相关测试；补配置头 EOF 换行。随后**不在此阶段构建或跑测试**，记录待末尾验证。 | 菜单/配置/布局/测试的静态调用关系一致；覆盖层其他调用点逐个核对，非只查一个哨兵文件。 |
| 3. 吸收剩余代码与非文档文件 | 按远程正序提交逐个取**最终仍存活的补丁**，能直接落到对应文件的直接吸收；已在本地实现或远程后来撤回的跳过。仅在代码结构冲突、重复定义或用户保留功能受影响时手工适配。先收尾已确认的 B4/B6 和 #27/#29/#40 差量，再处理新发现的具体补丁。保留本地 Zen、图标 MTSDF、自定义字体和独有加固；名牌 MTSDF 删除。 | 每个实际缺口有远程提交、补丁路径、落地文件和调用点；不执行整分支 merge，不按行差重写已有实现。 |
| 4. 提交表收口 | 更新上方 42 行的最终结论及未处理的**具体远程补丁**；混合提交中的非文档工作、合并节点第一父差异和最终删除单列核实，重复净项只记一次。 | 42 行可审查，遗留项必须指向明确补丁及消费者；历史表 12/13 不作为通过条件。 |
| 5. **代码全部完成后统一验证** | 同一 build 目录串行构建 `game-client`、`run_cxx_tests`、`qm-music-helper`；跑覆盖层等相关 Python/TS/Rust 测试与 default gate。失败则修代码/测试后重跑受影响项；CMake 更改须由 `game-client` 构建覆盖。 | `game-client`、`qm-music-helper`、C++ 3254/3254（490 suites）、Rust 全量通过；default gate 15/0/0/0（含 4 smoke、9 E2E）；构建/单测不替代端到端与人工视觉。 |
| 6. **代码全部完成后统一 i18n** | 按最终 Localize/Desc 集合整理 TOML 翻译来源，顺序执行 `extract_strings.py` → `generate_all.py` → `validate.py`；12 个 `data/languages/*.txt` 只由生成器更新。生成若改文件，再跑必要的受影响门禁。 | 全量提取新鲜、译文完整、blocking 0；警告及人工审阅积压如实保留。 |
| 7. **最后做运行时/人工验证** | 在可控开发实例检查覆盖层/称号/岛/好友/菜单视觉与配置；跨进程音乐、真实 WSS/语音/地图上传、焦点/窗口顶层、平台分支和性能帧时间分别核实。当前环境做不了的标“须人工”，不阻塞前面代码收口。 | 具体场景与结果；不可执行项列清条件，不把源码、构建或单测当作视觉/跨进程通过。 |
| 8. 交 review | 四目标绿且矩阵与 42 行闭合后更新归档表 8 与活文档最终状态；交用户 review，不推送、不建 PR。 | 表 8 与真实代码/验证一致，未验证项明确。 |

### 7.5 当前收口状态（2026-09-24）

| 阶段 | 当前状态 |
| --- | --- |
| 提交表收口 | **逐文件闭合已完成**。`tmp/closure_matrix.tsv` 覆盖 42 提交第一父差异的全部非文档路径；终结点有、本地无的 81 项全部归因（子模块 5、测试拆分 68、位图图标资产 4、脚本链差异 4、已内联代码 4、明确不吸收 2），本地保留 12 项为图标 morph 族。无遗留文件级漏搬。 |
| 统一构建/测试 | **已收口**。`game-client`、`qm-music-helper`、Rust 全量均退出码 0；C++ 3254/3254（490 suites）；default gate **15 通过 / 0 警告 / 0 失败 / 0 跳过**，4 个 smoke 与 9 个 E2E 均通过。第一次 gate 的 `connection_shutdown` 曾受 NVIDIA Vulkan 覆盖层外部崩溃影响，隔离复跑与第二次完整 gate 均通过，详见归档 10.266。 |
| 统一 i18n | **已收口**：`extract_strings.py` 0（5742 key）→ `generate_all.py` 0（12 文件）→ `validate.py` 0（`blocking violations: 0`；保留 4 条长度警告、335 项人工审阅积压）。 |
| 人工验证 | **未开始**：真实 WSS/语音/地图上传/跨进程音乐、视觉菜单与配置、焦点/窗口顶层、设备恢复、跨平台、性能帧时间均须人工；本轮未声称任何一项通过。 |
| 交 review | **自动收口条件满足，可交 review**：代码逐文件闭合、四目标/全量测试/default gate/i18n 均有当前工作树证据；真实 WSS/语音/地图上传/音乐进程、视觉、焦点/顶层、设备恢复、跨平台和性能仍如实标为须人工，不以自动结果代替。 |

2026-09-23 用户解除自动合并禁令、不另建 worktree，授权提交中间检查点，随后澄清只要**远程逐提交变更应用到本地对应文件**，不是整分支合并。可用 cherry-pick/三方 patch 生成候选，但零文本冲突也须检查是否重复实现；合并节点用第一父变更，侧支不重复计数。不回退/清理原成果，不推送、不建 PR。详归档 10.183。

2026-09-24 追加：名牌 MTSDF 生成脚本已从当前管线移除并改为图标专用命名：唯一入口仍是 `qmclient_scripts/qm_build_icon_msdf_font.py`，官方适配器为 `qmclient_scripts/qm_icon_msdf_official.py`，工具构建脚本为 `qmclient_scripts/qm_build_icon_msdf_official.py`；CMake 只保留 `qm-icon-msdf-atlas` 图标目标，不再存在 `qm_nameplate_msdf_official*.py`。适配器不是第二套名牌生成链，只服务图标 MTSDF 页面生成。

2026-09-24 新远程收口（`153bd251f8f8cd7b9f2060dc1551e4ef24db9e05..6b9a41fd21149cadab45650ace431ab4e7efca31`）：正序核对 8 个提交的第一父差异，共 95 条非文档路径；当前工作树 17 条与远程终点同字节，79 条为本地集成/适配后的合法分叉，无 `LOCAL_GONE` 路径，未发现新的代码漏搬。Zen/焦点、QmVulkan 增强渲染、图标 MTSDF、测试合同和皮肤网格修复均已落在当前代码；名牌 MTSDF 仍按裁决删除。

同日修复启动加载进度条黑色问题：`RenderLoadingDirect` 原把旧 `ui_color`（默认黑色）传给 Tee 进度条，现改用 `qm_ui_accent_color`。设置页迁移测试夹具同步从旧 `QmCardCatalog.cpp` 改为当前生产目录 `QmCardCatalogIds.cpp`。

当前最新自动验证：`game-client` 构建通过；C++ 全量 `3261/3261`（490 suites）；Python 脚本测试 89 项、9 skipped、0 failed；Rust 全量通过；`qm-music-helper` 目标通过；i18n `extract_strings.py`、`generate_all.py`、`validate.py` 通过，blocking violations 为 0；default gate（含 Qm smoke）`15 通过 / 0 警告 / 0 失败 / 0 跳过`。真实 WSS、语音双端、地图上传、音乐进程、当前工作区启动与视觉、OpenGL/Vulkan 驱动、窗口状态、设备恢复、跨平台和性能帧时间仍未人工验证，不能以自动结果替代。

同日代码审查新增一个确定的小缺口：Vulkan `QueuePresentKHR` 返回 `VK_SUBOPTIMAL_KHR` 时原 present 分支直接忽略，未把 `m_RecreateSwapChain` 置位；现已统一交给 `CheckVulkanCriticalError`，下一帧会排队重建。它与外部安装副本的 NVIDIA/覆盖层退出崩溃报告不是同一证据，后者仍须人工复验。

### 7.4 已知风险与不可控项（压缩保留）

| 风险 | 对策 |
| --- | --- |
| 引擎顶点/字体路径无自动化观察点（`text.cpp`、`glyph_atlas`/`glyph_outline`） | 结构向后兼容 + 单测锁默认行为，报告中标明须人工目视确认 |
| `map_vote_difficulty` 依赖失效计数器（`CVoting::OptionsRevision()`） | 已核对三处自增与选项列表真实变更点一一对应 |
| 破坏性删除（`island-layout/`、`docs/superpowers` 等） | 逐条核对本地用途后再动；名牌 MTSDF 的专属文件已逐项核对 Git blob 后删除，不使用整分支合并 |
| R1 依赖特定酷狗版本（CEF 调试点、`libcef.dll` 指纹） | 无法在本环境端到端验证的部分如实标注须人工 |
| i18n 新文案不进 TOML 会阻塞 | 新增文案进对应模块 TOML 并补 12 语后跑 `validate.py` |

## 八、新会话交接

在隔离 worktree `E:/Coding/DDNet/QmClient-local-first` 吸收 `2535814bd..3c4782e50` 的全部存活非文档工作，对照终点 `153bd251f` 共 42 提交。用户原始指令见第〇节；正序提交表见第三节；**当前执行方案见 7.3**；历史差异定位见归档表 12/13，现场审计见归档 10.178。

438 条未提交状态已归入中间检查点 `c34e1dd90`；整分支合并预览有 172 个冲突，实际合并尚未启动，见归档 10.182。自动合并禁令已解除，沿用本 worktree，不推送、不建 PR。第 178 轮旧 `OpenEditor()` 构建失败、覆盖层 Python 测试 5/6、quick gate 10/11、C++ 测试 3180/3180、i18n validate 通过；第 179–180 轮新改动**未重新验证**。旧“第 176 轮全绿”与“323 个剩余文件”均是历史快照。当前矩阵：872 路径、829 未逐文件判定、289 须实质语义审查、12 局部审理；完整分类及已确认的 B4 跨模块断点见归档 10.180。

`local_saves.h` 的确认删除与双人恢复整链已接本地 TClient、聊天及消息消费者并补行为测试（归档 10.192）。DDRace 地图进度算法/TClient/HUD 调试路线、demo 弹窗/播放器布局也已接本地消费者并补测试（10.194），**均未构建或运行**。本轮移除旧 `CQmRealtimeChannel`，账号/匿名实时 WS 分离，完善来源、身份、断线队列和称号重连订阅（10.197）；**仍未构建或实测服务端，不能宣称业务联通**。并行审计补脚本合同、地图搜索测试、岛边界、Vulkan sRGB 捕获、好友静默基线与音乐 helper 交付（10.198）；`ddnet-libs` 本地树含远程全部内容并额外保留 WavPack（10.199）。皮肤预裁切/逐帧上传/渲染信息恢复与 Vulkan 绘制量调度已接代码（10.201），网络/Axiom 继续审；其余 A/B/C/删除矩阵及混合提交仍待逐文件闭合，旧 872 行/829 未判定仅为过期定位快照。名牌 MTSDF 专属链已移除但未构建验证，自定义字体和图标 MSDF 保留。**所有代码与非文档工作之后**才统一构建、测试、gate、i18n 提取/生成/校验，最后做可执行的人工场景验证；本环境不能做的标“须人工”。完成后更新归档表 8，交用户 review。

2026-09-23 追加：网络接收泵与 Linux 配置目录、Axiom 记分板预取和持久化已有未验证改动；远程实时配置已部分接线，但日志开关尚无消费。完整明细见归档 10.202。当前工作树 176 条状态尚待检查点提交；不要以旧验证结果或旧矩阵推断剩余功能数。

2026-09-23 更新：176 文件检查点已提交为 `2cc1f1232`，后续图标/日志/测试批次为 `0b07fc74a`；`qm_websocket_log` 状态日志现已接，但旧 URL 显式默认值的覆盖优先级未判。逐提交 29 个三方候选仅 3 个文本零冲突：两项非文档差异为零，另一项 `crashdump.cpp` 可重复定义；整段试算 270 条 Git 冲突记录，均不可当剩余功能数。图标远程 32 个旧格式资源不直接搬入本地 MTSDF 管理器，HUD 两处字体回退已补；远程部分仍有效测试合入本地分拆测试。细节与证据见归档 10.203。新增改动均未构建/测试/i18n。

2026-09-23 续：好友分组/好友归属成功变更时补 `m_Revision` 并登记 `friends_revision_test.cpp`；脚本头文件护栏、门禁 `needs_base_ref` 与过滤行为已经本地等价，未覆盖本地 Python/smoke 检查。见归档 10.204。浏览器族仍在审；当前新改动未构建/测试/i18n。

2026-09-23 浏览器更新：`serverbrowser.cpp/.h` 与 `engine/serverbrowser.h` 的好友缓存改为条目自持，新增/替换地址推进好友列表修订号；`serverbrowser_test.cpp` 补行为断言。本地筛选与 HTTP 后台纯解析已有等价实现，未覆盖。细节见归档 10.205；代码、测试与性能仍未验证。

2026-09-23 续：终点净变更中当前字节不同约 290 路径，仅作定位，不等于未吸收功能数。字形默认面索引失效及轮廓裁剪、`qm-music-helper` 打包合同已补；UI 中断代理留下的表情释放、玩家/观战幽灵和传送点输入改动由主线程静态复核，未构建/测试/视觉验证。名牌、浏览器菜单及 editor 另行审查，详归档 10.206。

2026-09-23 续：名牌特效 LOD 改为每个实际绘制的名牌计一次；浏览器本地存档列表消费已有后台缓存、地图历史消费索引排序，避免 UI 线程同步读 CSV/复制记录。editor 的 14 个终点分叉文件中仅 `editor.cpp/.h` 的协作 HTTP→WSS 是确定缺口，其余 12 个远程存活意图在本地等价；server 三文件无确定缺口。编辑器 WSS 正在实现，均未构建/测试/真实服务验证；详归档 10.207。

2026-09-23 用户裁决：快速输入删除 Best 与 Saiko+，仅保留原始 Fast 输入路径；Zen 模式继续保留。已同步删除快速输入的 Best/Saiko+ 配置、预测/平滑分支、设置页三段模式控件、相关卡片预热哈希与失效源码合同，Fast amount/others/auto margin/Sub-Tick 保留。新增碰撞箱八项细分开关和歌词两个展示开关默认值已按远程终点恢复为 1；均未构建、测试或跑 i18n。详归档 10.208。

2026-09-23 续：并行审计已覆盖 #13–24、#25–31、#32 及 #33–42 的重点净项，**不是** 42 提交逐文件矩阵闭合。editor WSS 的重连、异步快照和广播帧槽位已补防护；聊天 emoji helper 接入实际排版，列表行高恢复 15；Tee 三卡布局 v9 迁移已接。当时把“梦”列不排序错记为远程意图，后续核对远程终点已接上 `SORT_QM_CLIENTS`（见归档 10.222）。`+emote` 释放先处理已在工作树；速通计时器此前已列为受保护本地卡片（归档 10.28），继续保留。`shot_emote` 与远程发射模式的语义、`tee_page_layout.toml`、图形资源重建广播触发点和 #35/#40 大合并节点仍需审理。图标 MTSDF 与递归字体目录保留，本地 HUD 的图标回退已有实现。此批当时均未构建、运行测试、gate、i18n 或实际服务/视觉验证；详归档 10.209。

2026-09-23 续：第 209 轮 30 文件检查点已提交 `f0353bb81`，提交不代表验证通过。#35 的 27 个非文档文件已逐项审理，四个明确缺口分配给不重叠的代码代理；#1–12 是提交级初审，不能误标逐文件完成。editor 切换地图标签时额外丢弃待上传快照，保留在途写盘任务的身份，避免在其他地图发布或误认普通保存；此处未构建或运行验证。六代理并行审 #1/#40 与四处实现，详归档 10.210。

2026-09-23 续：#35 四处代码已落地（表情选择器蓄力/发射、投射物窄通道与插值回退、HUD 红点 SDF/呼吸、浏览器单层视觉），尚未统一构建、测试或目视。#40 另找到搜索页仍是跳转列表及图标预布局重复绘制；后者已修，前者正在并行实现。#1 中途出现的 Axiom 他人弹窗在远程终点 `153bd251f` 已无符号，不能作为真实待办。此次纠偏和代码审阅详归档 10.211。

2026-09-23 续：搜索页现以目录卡直接编辑，并保留本地非目录条目的导航回退；`Locate` 翻译来源待最终 i18n 阶段。坐标名牌调用已有软重建 helper 并开启文本容器复用；旧圆角回绘源码镜像合同因生产结构已变且违反测试边界移除。#40 引擎审计仍有 `text.cpp` 的容器索引复制优化待吸收，独立代理正在处理。#2/#6 对照发现网易云恢复、旧歌词及 Android secret 转义为**两侧同有缺陷**，不是本次远程未吸收项；记录风险，不改变 42 行吸收结论。详归档 10.212，全部新增代码未验证。

2026-09-23 续：`text.cpp` 已改为内部只持有引用计数指针，不再复制完整文本容器索引；HUD SDF 参数构造失败加几何兜底。#35 已发现差量代码静态落地，#40 的其余分叉仍需逐文件证据；#4/#7/#8/#9 的独立提交审计继续。当前未构建、测试、gate、i18n 或运行时/视觉验证，详归档 10.213。

2026-09-23 续：本批将酷狗/QQ 独立开关纳入 HUD 歌词来源判定；畸形 QRC 包装不再被当作裸 rlrc 回退。Spotify `sp_dc` 输入行已与 Lyrics 卡测量、预布局对齐并固定遮蔽；DebugMode/BindStatusHud 测量按真实五/六行修正。`git diff --check` 退出 0；代码尚未构建/测试，i18n 尚未统一生成，详归档 10.214。两份其他计划文档的未计划改动不纳入本批。

2026-09-23 检查点：上述 19 文件改动已提交 `e9935da09`，`git diff --cached --check` 退出 0。提交不是验证结论；#5/#29 的独立审计也已启动。
