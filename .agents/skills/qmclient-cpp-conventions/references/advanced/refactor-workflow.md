# 重构参考

只为当前任务中实际存在的重复、难以验证的逻辑、生命周期问题或热点重构；遵循附近 DDNet 模式，保持补丁聚焦。

先识别必须保持的公开行为、配置、日志合同及调用顺序；有不确定性时继续读实现和测试。只将需要协作或长期保留的边界写入文档，不强制列举所有不修改的文件。

以可审查的小步修改真实降低复杂度的部分，完成一个可验证的逻辑单元后做相关检查；不要为每次改名或抽函数重复全量测试。验证范围与证据复用统一按 `qmclient-verification-gate`。

交付说明结构改善、行为保持依据和实际限制即可，不要求列出被延后的无关重构建议。
